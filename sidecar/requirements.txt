torch --index-url https://download.pytorch.org/whl/cpu
torchvision
transformers
huggingface_hub
pymupdf
pillow
# GLM-OCR SDK (optional, MaaS cloud API or self-hosted backend only):
# pip install git+https://github.com/zai-org/GLM-OCR.git
# glm-ocr
# optional for speed: vllm or sglang or ollama
